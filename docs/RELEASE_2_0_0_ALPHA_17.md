# Vaultarr 2.0.0 Alpha 17 — Orbital Login Overlay

The login screen now uses the Orbital Core panel itself as the form surface. The orbital animation remains fully visible while the login labels, translucent inputs, and action button are layered directly over it. The separate opaque glass card has been removed. Authentication logic is unchanged.
