# Vaultarr 2.0.0 Alpha 12 — Visible Scan Shimmer

This patch fixes the Museum Scan pill appearing unchanged while a scan is running. The prior sweep could be suppressed by reduced-motion settings or become nearly invisible through blend-mode compositing. Alpha 12 uses a stronger blue ribbon rendered directly above the pill background and retains a slower sweep under reduced-motion settings.
